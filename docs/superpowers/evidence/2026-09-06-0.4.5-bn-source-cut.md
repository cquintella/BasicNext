# Bucket 0.4.5 — `bn_source` leaf cut

Date: 2026-09-06

## Implemented

- Extracted `SourceId`, `Revision`, `Position`, `Span` and `SourceFile` into the
  standalone `bn_source` workspace crate.
- Kept the root API compatible through `pub use bn_source as source`.
- Declared the crate as a leaf: it has no dependency on the frontend, semantic
  model, IR or backends.

## Verification

```text
cargo fmt --check
passed
cargo check --workspace --all-targets
passed
cargo build -p bn_rt
passed
cargo test --workspace --exclude bn_rt
passed: root tests 176 + 4, CLI 60, codegen 11, heap 3, IR 9,
lexer 4, module graph 20, parser 20, runtime 172, semantic 48,
sprint closure 5, U1 5, validated IR 8, plus bn_source/doc tests
bash tests/check-forbidden-deps.sh
passed (including the seeded negative fixtures)
```

This is the first hard-split leaf, not closure of GC-FE, GC-DEP or G3a.
CLI/LSP session wiring, full span propagation, remaining crate cuts and the
complete `bn_rt` network test suite remain open.
