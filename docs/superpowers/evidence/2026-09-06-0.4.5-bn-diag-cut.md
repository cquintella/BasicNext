# Bucket 0.4.5 — `bn_diag` leaf cut

Date: 2026-09-06

## Implemented

- Extracted the shared `Diagnostic` fact/render primitive into `bn_diag`.
- Made it depend only on `bn_source` for source spans and source rendering.
- Preserved the root-facing `crate::diagnostic` path through a reexport.
- Kept frontend, semantic, IR and backend implementations out of the leaf.

## Verification

```text
cargo fmt --check
passed
cargo check --workspace --all-targets
passed
cargo test --test validated_ir
passed: 10 tests
cargo clippy --workspace --all-targets -- -D warnings
passed
```

This is a dependency-boundary cut only. Fluent catalogs, `DiagnosticSpec`,
registry/policy and full G4 migration remain open.
