# Evidence Note: 2026-09-07 Gate G4a Random Honesty (F-04)

* **Date:** 2026-09-07
* **Author / Owner:** Doug
* **Target Gate:** Gate G4a (F-04)
* **Status:** Passed / Fixed

---

## 1. Activities Summary

* **Activity 4a.1: Document HOST.Random as non-cryptographic PRNG**
  * Updated `docs/architecture/nfr-security.md` (§7 Random honesty):
    * Documented that `HOST.Random` (`Random`, `Seed`) is an explicitly non-cryptographic PRNG (xorshift64*/LCG-based in `src/runtime/executor/part7.rs`).
    * Prohibits use for cryptographic keys, tokens, nonces, and session identifiers.
  * Verified CSPRNG boundary:
    * HTTP session IDs (`new_session_id`) and request IDs (`new_request_id`) in `src/web_state.rs` consume `SystemEntropy` backed directly by `ring::rand::SystemRandom` (CSPRNG).
    * Grep / code audit confirms zero usage of `HOST.Random` for session or request generation.

---

## 2. Test & Code Verification

* `src/runtime/executor/part7.rs::host_call`:
  * `HOST.Random.Random` and `HOST.Random.Seed` operate on an atomic `u64` state.
* `src/web_state.rs`:
  * `SystemEntropy` implements `EntropyProvider` using `ring::rand::SystemRandom`.
  * `tests::session_ids_are_random_and_have_at_least_128_bits` passes.
  * `tests::entropy_failure_does_not_insert_a_session` passes.
  * `tests::request_ids_are_bounded_and_entropy_backed` passes.
