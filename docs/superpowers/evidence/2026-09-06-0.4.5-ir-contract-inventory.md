# Bucket 0.4.5 — IR contract inventory

Date: 2026-09-06

## Implemented

- Synchronized `docs/architecture/ir-contract.md` with the complete current
  `Instruction`, `Terminator` and `Constant` surface in `src/ir/model.rs`.
- Documented every instruction's result, every `ValueId` operand (including
  `Input.prompt` and dynamic dimensions), and its effect/validation notes.
- Documented the current dense block-ID invariant, CFG operands and the
  explicit `Phi` predecessor rule.
- Declared the compatibility boundary honestly: no serialized-IR compatibility
  is promised in 0.4.5; the in-memory model is versioned by the toolchain
  release until a serialized schema is accepted.

## Verification

```text
cargo test --test validated_ir
passed: 8 tests
```

The inventory closes Activity 2.1 for the current slice. It does not close
GC-IR, GC-SUP or GC-DEP: full operand/type validation, target coverage and the
`bn_ir` crate boundary remain delivery work.
