# Evidence Note: 2026-09-07 Gate G4b Filesystem Default & TOCTOU (F-05)

* **Date:** 2026-09-07
* **Author / Owner:** Doug
* **Target Gate:** Gate G4b (F-05)
* **Status:** Passed / Fixed

---

## 1. Activities Summary

* **Activity 4b.1: Lock default for untrusted HostEnv: fail-closed / rooted**
  * Added `HostEnv::sandbox(arguments: Vec<String>) -> HostEnv` in `src/runtime_impl.rs`:
    * Initializes `FilesystemPolicy::denied()`, which rejects all reads, writes, and capability checks by default.
  * Documented in `docs/architecture/nfr-security.md` (§8 Filesystem default sandbox and rooted TOCTOU):
    * Untrusted execution profiles default to fail-closed / denied filesystem access (`HostEnv::sandbox`).
    * `HostEnv::system` is documented as an explicit grant of full host trust.

* **Activity 4b.2: Rooted mode: mitigate symlink escape**
  * Path checking in `FilesystemPolicy::allows_path` canonicalizes target paths (`path.canonicalize()`) before comparing against granted root prefixes.
  * Verified via `runtime::tests::filesystem_policy_mitigates_symlink_escape`:
    * Creating a symlink inside a rooted directory pointing to an outside file is strictly denied access.
    * Path resolution resolves symlinks before evaluation, preventing symlink traversal out of the sandbox.

---

## 2. Test Verification

Tests executed:
* `runtime::tests::host_env_sandbox_defaults_to_fail_closed_filesystem`
* `runtime::tests::filesystem_policy_mitigates_symlink_escape`
* `runtime::tests::filesystem_policy_separates_read_and_write_roots`

All tests pass without failures.
