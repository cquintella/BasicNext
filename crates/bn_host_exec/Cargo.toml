[package]
name = "bn_host_exec"
version.workspace = true
edition.workspace = true
rust-version = "1.97"
description = "Basic Next HOST.Exec core: one process-execution implementation shared by the interpreter and the native runtime"
license.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = "warn"
pedantic = "warn"
