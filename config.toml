# Basic Next local tool configuration.
# Language semantics and standard-module layout remain defined by the 0.2 contract.

[toolchain]
clang = "clang"

[mcp_servers.github]
bearer_token_env_var = "GH_TOKEN"
