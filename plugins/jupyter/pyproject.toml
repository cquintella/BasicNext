[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bn-kernel"
version = "0.4.3"
description = "Jupyter host for Basic Next"
requires-python = ">=3.10"
dependencies = ["pyzmq>=25"]

[tool.setuptools.packages.find]
include = ["bn_kernel*"]
