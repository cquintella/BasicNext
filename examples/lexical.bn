REM Lexical 0.1: identifiers are case-sensitive.
/* Block comments may nest.
   /* Nested comment. */
*/
IMPORT HOST.main AS main

SUB Start()
    LET message AS STRING = "Basic Next" // This is also a comment.
    LET counter AS INTEGER = 1

    PRINT message, counter
END SUB
