# Basic Next 0.4.4 Soft Preparation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Execute the 0.4.4 soft-preparation bucket in order, starting with an evidence-backed FE/IR/BE ownership baseline and preserving the validated IR boundary.

**Architecture:** Keep the current single Rust package while documenting the target layered DAG. Record current paths and known deviations as debt; add enforcement and validation only after the ownership baseline is reviewable.

**Tech Stack:** Rust 2024, Cargo, Markdown, `rg`, repository test and lint commands.

**Spec:** `ongoing/bucket-0.4.4.md`, `docs/architecture/target-architecture.md`, `audit/workpapers/09-synthesis/fe-be-split-milestones.md`

## Global Constraints

- Do not add language keywords, HOST capabilities, or IR instruction kinds.
- Keep lowering frontend-owned; backends consume validated IR.
- Do not mark an activity done without reproducible evidence.
- Preserve unrelated working-tree changes.

### Task 1: Establish the current ownership inventory

**Files:**
- Create: `docs/architecture/ownership.md`
- Modify: `docs/architecture/README.md`
- Modify: `ongoing/bucket-0.4.4.md`
- Modify: `ongoing/WBS-0.4.md`

- [X] **Step 1: Map current source paths to FE, IR, BE, and EDGE ownership.**
- [X] **Step 2: Record target ownership and current deviations with exact paths.**
- [X] **Step 3: Link the canonical inventory from the architecture README.**
- [X] **Step 4: Run link/reference and `git diff --check` verification.** Repository-wide `git diff --check` still reports pre-existing trailing whitespace in unrelated modified architecture files.
- [X] **Step 5: Update S0.1/WBS 7.1 evidence only after review.**

### Task 2: Enforce forbidden dependency directions

**Files:**
- Create: repository dependency-check script and its focused test fixture
- Modify: `ongoing/bucket-0.4.4.md`

- [X] Add a finite allowlist for existing debt and reject new backend-to-frontend edges.
- [X] Verify the checker rejects a temporary illegal import and accepts the baseline.

### Task 3: Record driver/baseline and validate the IR bridge

**Files:** `docs/architecture/`, `tests/`, `src/ir/`, `ongoing/`

- [ ] Trace CLI paths and record LSP/DAP divergence.
- [ ] Capture reproducible baseline results.
- [ ] Add W1 boundary and S1.2 negative/positive validation fixtures.
- [ ] Run the required Rust gates and update evidence.
