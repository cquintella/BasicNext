standard library (I/O, collections, Unicode helpers).
LSP (Language Server Protocol) like pyright
Formatter A tool that formats source code to a consistent, opinionated style automatically.
Debugger (and DAP support): A runtime debugger (or adapter exposing the Debug Adapter Protocol) that supports breakpoints, step-over/into/out, variable inspection, call stacks, and watch expressions.
Test runner:A tool / convention to define, run, and report unit/integration tests, possibly with fixtures, assertions, and coverage reporting like pytest
CI templates: Reusable continuous integration workflow configurations (e.g., GitHub Actions, CircleCI) that build, test, lint, and release projects automatically.
Add add cycle detection to ARC
Improve error ergonomics by attaching clear spans, a one-sentence cause, one high-value suggestion, and actionable LSP code-actions for quick fixes; prioritize the common errors users encounter early.
