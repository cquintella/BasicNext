[package]
name = "bn_support_matrix"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Basic Next support-matrix inventory and gap report"
license.workspace = true
publish = false

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = "warn"
pedantic = "warn"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
