# Bucket 0.4.5 — `bn_types` leaf cut

Date: 2026-09-06

## Implemented

- Extracted `Type`, `IntegerType`, `FloatType`, `PointerLength`, `ModuleId` and
  their size/dimension helpers into the standalone `bn_types` workspace crate.
- Preserved the existing root-facing path through `pub use bn_types as types`.
- Kept the leaf free of frontend, semantic, IR and backend dependencies.

## Verification

```text
cargo fmt --check
passed
cargo check --workspace --all-targets
passed
cargo test --test validated_ir
passed: 10 tests
cargo clippy --workspace --all-targets -- -D warnings
passed
```

This is preparatory progress for GC-DEP and the `bn_ir` extraction. It does
not close GC-DEP because the in-tree IR module still contains lowering and
frontend conversion code.
