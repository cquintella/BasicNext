# Basic Next 0.4.4 executable baseline

Recorded on 2026-09-05 before starting S1 of `ongoing/bucket-0.4.4.md`.

## Identity and environment

- Repository: `/Users/caq/src/BasicNext`
- Commit: `ba7f07ac7f7d9244090a02e5f1906db26ec39718`
- Working tree: dirty before this evidence was added; 29 tracked paths modified/deleted and 13 untracked paths were present.
- Tracked patch SHA-256 (`git diff --binary`): `901559940cbb3c07969d20d1e102c416b44e01600ac85bbf4746aff008e07b59`
- Untracked-path SHA-256 (sorted path list): `8d77a5ca9aa956f77e9bbb4d2694db5e2af2faf48115b60cdb52dfb440be446c`
- Rust: `rustc 1.97.0 (2d8144b78 2026-07-07)`; Cargo `1.97.0 (c980f4866 2026-06-30)`
- Python: `3.14.4`; Node: `v24.11.1`; clang: `23.1.0`
- `wasm-ld`: unavailable on this host. The wasm parity suite still ran using its configured path/fixtures.

## Commands and results

| Command | Result | Disposition |
| --- | --- | --- |
| `tests/check-forbidden-deps.sh` | PASS; baseline and seeded illegal-import test passed | S0.2 evidence |
| `cargo fmt --check` | PASS | Required quality gate |
| `cargo clippy --locked --all-targets -- -D warnings` | PASS | Required quality gate |
| `cargo test --locked --all-targets -- --test-threads=1` | FAIL in `tests/u1.rs`: 2 tests | Existing documentation/workflow debt; see below |
| `git diff --check` | FAIL: 5 trailing-whitespace lines in pre-existing modified architecture files | Existing hygiene debt; not introduced by S0.4 |
| `python3 -m unittest tests/test_capabilities.py` | FAIL: 3 capability expectation mismatches | Product parity work; route to Section 4 Activities 4.1/4.2 |
| `python3 -m unittest tests/test_compiler_parity.py` | PASS (5 tests) | Capability parity evidence |
| `python3 -m unittest tests/test_wasm_parity.py` | PASS (2 tests) | Wasm parity evidence |

## Failure dispositions

`tests/u1.rs::done_tree_is_closed_and_todo_accepted_docs_are_pointers` reports
an unchecked gate in `done/bucket-0.4.3.md`. The working tree also contains
deletions of the historical `ongoing/0.4-*.md` files, while many existing
architecture/WBS links still point to them; `tests/u1.rs::markdown_links_resolve`
reports those broken links. These are repository-documentation reconciliation
items, not S0.4 implementation failures. They remain visible and block a
clean all-target baseline; destination: 0.4.4 release evidence / G4.

The capability suite reports three reproducible product mismatches: compiled
`examples/hello.bn` output differs from its declared output, the KMP expected
fragment omits the actual separator, and `print-if-or.bn` builds when the
capability fixture expects rejection. These belong in the frozen matrix and
example report, destination: Section 4 Activities 4.1 and 4.2; no assertion
was weakened to make the baseline green.

This baseline is evidence for S0.4 and does not claim G4, G-SOFT, or a clean
release.
